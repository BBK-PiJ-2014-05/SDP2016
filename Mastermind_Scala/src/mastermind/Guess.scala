package mastermind

/**
  * Created by geoff_000 on 02/03/2016.
  */
class Guess {

  // each guess has n elements where n is the size of the codeSet (in this case 4)
  // the player enters n chars to a guess String (from System.in or Scala equivalent)
  // when n chars have been entered the method stops taking input
  // check the input for a valid entry
  // this could be a recursive function on an immutable List; each time a new List is returned
  // with the old list plus the new choice




}
